%p{:style => "color: green"}= notice
.div.m-4
  .page.header 
    %h1.mb-4
      = t('application.list_of', model: t('activerecord.modules.doctor.other'))
    .float-end.pb-4
      = link_to t('application.create'), new_doctor_path, class: "btn btn-primary"

%table.table.table-striped
  %thread
    %tr
      %th= t('activerecord.attributes.content.first_name')
      %th= t('activerecord.attributes.content.last_name')
      %th= t('activerecord.attributes.content.code')

  %tbody
    - @doctors.each do |doctor|
      %tr
        %td= doctor.first_name
        %td= doctor.last_name
        %td= doctor.code
        %td= link_to t('application.show'), doctor, class: "btn btn-info"
        %th= link_to t('application.edit'), edit_content_path(doctor), class: "btn btn-success"
        %th= link_to t('application.delete'), doctor, class: "btn btn-danger"

%br
