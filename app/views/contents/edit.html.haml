%h1= t('application.editing', model:t('activerecord.modules.content.one')) 
= render "form", content: @content
%br/
%div
  = link_to t('application.show') +" "+ t('application.this')+" "+ t('activerecord.modules.content.one'), @content
  |
  \#{link_to t('application.back_to',model:t('activerecord.modules.content.other')), contents_path}
